---
- hosts: local
  tasks:
  - debug:
      msg: "{{ greetings }}"
      verbosity: 0
  - shell: "touch ./file.txt"
  - shell: "echo 'omg' > ./file.txt"
  - file:
    src: _attachments/whut.json
    state: touch
  
